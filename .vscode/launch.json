{
    "version": "0.2.0",
    "configurations": [
        /*
         * Configuration: Pure C++ Debugging.
         * Purpose: Debug standalone C++ executables.
         * Use: When testing C++ components without Python
         * Note:
         *   - Customize `"program"` entry to your C++ executable path.
         */
        {
            "name": "Pure C++ Debug",
            "type": "cppdbg",
            "request": "launch",
            // Customize `"program"` entry to your C++ executable path.
            "program": "${workspaceFolder}/build/bin/toyc-mytoy",
            "args": [
                "${workspaceFolder}/tests/Ch7/struct-ast.toy"
            ],
            "stopAtEntry": true,
            "sourceFileMap": {
                "${workspaceFolder}/build": "${workspaceFolder}"
            },
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                },
            ],
            "environment": [
                {
                    "name": "EXAMPLEKEY",
                    "value": "EXAMPLEVALUE"
                }
            ]
        },
    ],
}
